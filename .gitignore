# ไฟล์แนะนำในแต่ละโฟลเดอร์รูป (ไม่ต้องขึ้นเว็บ)
_ใส่รูปที่นี่.txt
_ใส่วิดีโอสั้นที่นี่.txt
_ใส่เอกสารที่นี่.txt

# ระบบ
.DS_Store
Thumbs.db
desktop.ini

# วิดีโอไฟล์ใหญ่ — แนะนำใช้ YouTube (เอา # ออกถ้าอยากกันไฟล์ mp4 หลุดขึ้น repo)
# *.mp4
# *.mov
